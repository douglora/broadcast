# Dependencias do runner do livro (GitHub Actions). Nao misturar com o
# requirements.txt do terminal, que o app.py instala no computador do Douglas.
requests>=2.31
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
curl_cffi>=0.7
trafilatura>=1.9
pypdf>=4.0
